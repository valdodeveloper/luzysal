---
const { color = '#f5f5f5', title = 'Título', avatar, children } = Astro.props;
---
<html lang="es" class="h-full">
  <head>
    <link rel="icon" type="image/png" href="/music_icon.png" />
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
  </head>
  <body class="min-h-screen flex flex-col items-center justify-center" style={`background:${color}`}>  
    {avatar && <img src={avatar} alt="Avatar" class="w-24 h-24 rounded-full mb-4 shadow-lg" />}
    <main class="w-full max-w-md p-6 rounded-xl bg-white/80 shadow-xl">
      {children}
    </main>
  </body>
</html>
<style is:global>
@import '../assets/styles/tailwind.css';
</style>
